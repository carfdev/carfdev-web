---
import { Link } from "@/components/ui/link";
import { ScrollReveal } from "@/components/ui/scroll-reveal";
import { getLangFromUrl, useTranslations } from "@/i18n/index-ui";
import { Calendar } from "lucide-react";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

const cta = t("cta");
---

<section id="cta-banner" class="bg-primary px-6 py-20 lg:px-12">
  <ScrollReveal client:visible className="mx-auto max-w-4xl text-center">
    <h2 class="text-secondary">{cta.title}</h2>
    <p class="subtitle text-muted mb-8">
      {cta.subtitle}
    </p>
    <div class="flex flex-col items-center justify-center gap-4 sm:flex-row">
      <Link href="#contact" size="lg" variant="secondary">
        <Calendar />
        {cta.buttonText}
      </Link>
    </div>
  </ScrollReveal>
</section>
